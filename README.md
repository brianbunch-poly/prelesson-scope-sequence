# Prelesson Game Scope & Sequence

Static dashboard for browsing prelesson game scope & sequence content.

## Cloudflare Pages

1. Connect this repository in Cloudflare Pages.
2. Build settings: leave build command empty; output directory `/` (or leave blank).
3. The site entry point is `index.html`.

Asset checklist progress is stored in each browser's localStorage.
